# Mission Control Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# GATEWAY CONFIGURATION
# =============================================================================

# WebSocket URL for real-time communication with the Mission Control Gateway
# This should be a PUBLIC URL that browsers can access (NOT an internal URL)
#
# For Railway deployment:
#   1. In your Railway dashboard, go to your gateway service settings
#   2. Under Domains, add a custom domain or use the generated public domain
#   3. Set this variable to: wss://your-gateway.railway.app
#
# For local development:
#   - If using ngrok: wss://abc123.ngrok.io
#   - If using localtunnel: wss://your-app.loca.lt
#   - Direct localhost: ws://localhost:8080 (only works with Next.js dev server)
#
# IMPORTANT: 
#   - Use wss:// for production (Railway auto-upgrades ws:// to wss:// on your domain)
#   - Do NOT use internal Railway URLs like "*.railway.internal"
#   - Browsers cannot access internal Railway URLs
NEXT_PUBLIC_GATEWAY_WS_URL=

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Mission Control REST API base URL
# Used as fallback when WebSocket connection fails (HTTP polling mode)
#
# For Railway: https://your-api.railway.app
# For local: http://localhost:3001 (or your API port)
NEXT_PUBLIC_MC_API_URL=

# =============================================================================
# OTHER OPTIONAL VARIABLES
# =============================================================================

# Next.js App URL (used for redirects, webhooks, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Enable detailed debug logging in browser console
NEXT_PUBLIC_DEBUG=false
